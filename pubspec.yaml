name: rc_router
description: A simple but powerful Flutter router.
version: 1.0.1
homepage: https://rushio-consulting.fr

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  provider: ^6.0.1
  pedantic: ^1.11.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^1.0.4
