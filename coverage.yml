coverage:
  status:
    # pull-requests only
    patch:
      default:
        threshold: 0.1%

ignore:
  - "lib/src/**/*.g.dart"

fixes:
  - "::coverage/"         # move root   e.g., "path/" => "after/path/"

comment:
  layout: "reach, diff, flags, files"
  behavior: new
